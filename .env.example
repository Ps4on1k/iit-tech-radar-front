# Frontend Environment Variables
# Скопируйте этот файл в .env.local для локальной разработки

# API URL (опционально, по умолчанию используется proxy /api)
# VITE_API_URL=http://localhost:5000/api

# ===========================================
# PRODUCTION настройки (для ВМ)
# ===========================================
# Для production сборки в GitHub Actions установите:
# VITE_API_URL=https://<ваш-домен>/api
